import styled from 'styled-components'
import { INSET_MARGIN } from '../helpers/layout'

export const MarginWrapper = styled.div`
  padding-left: ${INSET_MARGIN}px;
  padding-right: ${INSET_MARGIN}px;
`
